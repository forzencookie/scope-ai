"use client"

/**
 * AIChatPanel - Legacy component
 * Chat functionality has been moved to:
 * - ChatProvider (src/providers/chat-provider.tsx) — shared state
 * - MainContentArea (src/components/layout/main-content-area.tsx) — chat UI
 * - ChatHistorySidebar (src/components/layout/chat-history-sidebar.tsx) — history
 *
 * This file is kept as a stub. The component is no longer used in the layout.
 */

export function AIChatPanel() {
    return null
}
